# SonarQube local — java-pleno-pixel-hunt

Instância compartilhada. Credenciais canônicas: `../../repo/sistema-folha/_docs/sonarqube-local.md`.

| Campo | Valor |
|-------|-------|
| URL | http://localhost:9000 |
| projectKey | `java-pleno-pixel-hunt` |
| Config | `sonar-project.properties` |
| Token local | `.sonar.env` (copiar de `.sonar.env.example`) |
| USER_TOKEN | `agent-local-20260827` |
| SonarLint | `.sonarlint/connectedMode.json` |

**MCP / code-review:** USER_TOKEN global em `~/.cursor/mcp.json`.

**QG (2026-08-27):** ERROR — 3 `new_violations`; 22 vulns S2245 em `app/page.tsx`.
