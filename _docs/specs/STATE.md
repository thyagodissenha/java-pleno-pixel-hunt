# Estado do Projeto e Registro de Decisões (STATE.md)

## Decisões Ativas de Arquitetura (ADR / Decisions Log)

| ID | Data | Título | Decisão | Status |
| --- | --- | --- | --- | --- |
| `AD-001` | 2026-08-27 | Organização das Especificações | Utilização do layout flat `_docs/specs/` com `tlc-spec-driven` | Confirmado |
| `AD-002` | 2026-08-27 | Integração com SonarQube | Projeto `java-pleno-pixel-hunt` registrado no SonarQube local em `http://localhost:9000` | Confirmado |
| `AD-003` | 2026-08-27 | Escopo da Fase 2 | Foco em Estabilidade, Qualidade de Código, Modo Debug e Cobertura por Testes | Em Andamento |
| `AD-004` | 2026-08-29 | Janela de dedupe assimétrica (ledger 24h vs ranking top 10) | Resolvido via quick fix: comportamento aceito por design (dedupe escopado a "enquanto o score permanecer no ranking"), fixado por teste de regressão em `high-scores.test.ts`. Ver `spec.md` seção "Quick Fix — 2026-08-29" e `validation.md`. | Confirmado |
| `AD-005` | 2026-08-29 | Quick fix `ESTAB-19` AC4 | Teste de `score-abuse-preflight.ts` passou a derivar a decisão do texto real do script Lua (em vez de reimplementar a cota em JS); mutante `count <= limit` confirmado morto. | Confirmado |
| `AD-006` | 2026-08-29 | Quick fix apodrecimento de teste `readRankingSnapshot` | Teste "returns snapshot ETags..." passou a passar `now` explícito em vez de depender do relógio real; corrige falha por passagem de calendário sem relação com código de produção. | Confirmado |
| `AD-007` | 2026-08-29 | Bug de produção: ranking duplicado visualmente | Causa raiz: `GET /api/scores` reaplicava `sanitizeScore` (que sempre estampa `createdAt: now`) sobre scores já persistidos, colapsando o `createdAt` de todos os registros e quebrando a `key` React no cliente. Corrigido com `sanitizePublicScore` (preserva `createdAt` válido). Reportado pelo usuário em produção, confirmado via JSON cru de `/api/scores`. | Confirmado |
| `AD-008` | 2026-08-29 | `app/page.tsx` não aceita exports nomeados extras | `app/page.tsx` é um arquivo de rota do Next.js App Router — o type-check gerado (`.next/types/app/page.ts`) só permite um conjunto fixo de exports nomeados (`default`, `metadata`, `generateStaticParams` etc.). Qualquer função/constante pura que precise ser exportada para teste direto SHALL viver em `lib/` (ou outro módulo não-rota), nunca ser exportada direto de `app/page.tsx`. Descoberto durante Execute de `estabilidade-qualidade-fase2` T1 (`npm run build` falhava com `TS2344` ao exportar `obstacleCount`). | Confirmado |
| `AD-009` | 2026-08-30 | Temas de HUD: split presentacional, motor não migra | O motor do jogo (estado/refs/o `useEffect` de física de ~1300 linhas) permanece em `app/page.tsx`, inalterado — só a JSX de apresentação (topbar/bottombar/telas de menu) é extraída para componentes de tema fora de rota (`app/_hud/[tema]/`, prefixo `_` ignorado pelo App Router), recebendo um único tipo `HudProps` do motor. Escolhido em vez de extrair o motor inteiro pra um hook (`useGameEngine`) por risco: mover o `useEffect` gigante teria superfície de regressão alta demais pra um jogo já publicado (ver `AD-006`/`AD-007`, bugs nascidos de refactors menores). Qualquer tema futuro segue o mesmo padrão `app/_hud/[tema]/`. Ver `_docs/specs/features/sistema-de-temas-hud/design.md`. | **Revogada por `AD-010`** |
| `AD-010` | 2026-09-08 | Revogação da AD-009 — motor extraído para `lib/pixel-hunt-engine/` orientado a Phases | Decisão explícita do usuário: o motor de jogo (física, colisão, spawn, IA, renderer) sai de `app/page.tsx` e vai para `lib/pixel-hunt-engine/`, sem nenhuma dependência de React (`setState`/hooks), organizado em torno de uma interface `Phase` genérica que todo encontro do jogo (ondas normais, chefe, escolha final, fase secreta "O Mainframe") implementa. Motivação: permitir expandir o jogo (novas fases futuras) sem duplicar spawn/física/render, e tornar o motor testável isoladamente (Vitest, sem montar componente React) — algo impossível hoje. Risco de regressão (a mesma razão que gerou a AD-009) é assumido conscientemente e mitigado por paridade de comportamento obrigatória, cobertura de teste nova (unitário + E2E) e QA manual antes do deploy. Ver `_docs/specs/features/motor-de-jogo-modular/spec.md`. | Confirmado |

---

## Handoff Snapshot

- **Feature Atual:** `separar-golive-diretoria` — **Verified (PASS) e mergeada em `main`** via squash. Separa o chefe da onda 4 ("Diretor do Go-Live", agora 1 barra de HP definitiva, mesma mecânica genérica das ondas 1–3) do chefe da onda 5 (a "Diretoria", mecânica de 3 sub-fases escaláveis preservada, só deslocada de onda 4 → onda 5, que agora leva à tela de Escolha Final). Cobriu 11 requisitos (`GOLIVESPLIT-01` a `11`). Confirma o investimento de `AD-010`: toda a mudança de código foi crescer o roster (`bossNames`/`biomeNames` de 4 para 5 entradas) + um 5º tema visual ("Sala do Conselho") — `graph.ts`, `boss.ts`, `waves.ts` e `physics.ts` não precisaram de nenhuma mudança de lógica, só de dados, porque já derivavam de `bossNames.length`. Efeito colateral: o mob "Legado" (liberado a partir de `wave >= 5`, antes código morto) passa a ser alcançável em jogo pela primeira vez. Ver `_docs/specs/features/separar-golive-diretoria/spec.md`.
- **Fase Atual:** Execute concluído (7 tasks, 1 worker em background, commits `477d070`..`63c4bc1`). Verifier Rodada 1: **FAIL** — 6/11 ACs com evidência exata, 1 spec-precision gap, 4 ACs sem nenhuma citação de teste (`GOLIVESPLIT-04, 06, 08, 09, 10`) — nenhuma regressão funcional confirmada, todo código de produção lido e verificado correto, gaps eram exclusivamente de cobertura automatizada. Ciclo de fix 1/3 (`c08946f`, `092070d`, `24d726a`, `5c20264`): fechou os 5 gaps só com testes novos + 1 `export` de visibilidade (`computeBossVolleyPlan`, antes privada). Verifier Rodada 2: **PASS** — 11/11 ACs, 449/449 testes, sensor de discriminação 3/3 mutações mortas, build ok; `npm run test:e2e` bloqueado pelo mesmo lock de ambiente `.next/dev` de sessões anteriores (não é falha de código). `/code-review` (nível medium, 8 finder agents + verify) encontrou 2 achados **PLAUSIBLE** classificados (c) — melhoria fora dos ACs, não implementados: (1) `computeBossVolleyPlan` usa `bossIndex % 4` hardcoded, sem vínculo a `bossNames.length` — landmine só se uma 6ª onda for adicionada no futuro; (2) a função recém-exportada muta `enemy.cooldown` como efeito colateral não sinalizado, risco só para um chamador futuro hipotético. Ambos registrados em "Questões abertas" no `spec.md`. JaCoCo não se aplica (projeto TypeScript/Next.js); SonarQube MCP falhou ao conectar nesta sessão (`CONNECT_TIMEOUT`) e não há `sonar-scanner` instalado localmente — análise não rodada, mesma limitação pré-existente (SonarQube só cobre `main`).
- **Branch:** `feat/separar-golive-diretoria` (push feito, `origin` atualizado) — squash-mergeada em `main` como o commit `652f517` (`main` antes: `a96799d`). Gate rodado direto em `main` antes do push: `npm run build` ok; `npm test` teve 1–3 falhas intermitentes em `app/__tests__/score-sync.test.tsx` (testes de timing de sincronização offline, não relacionados a este diff, arquivo não tocado por esta feature, confirmado passando 16/16 quando rodado isolado) — tratado como flakiness pré-existente da suíte sob carga, não bloqueante.
- **Pendências:** Nenhuma da feature em si. `npm run test:e2e` não rodou nesta sessão (mesmo bloqueio de ambiente recorrente, ver acima). A flakiness de `score-sync.test.tsx` sob execução da suíte completa é uma observação nova para investigar depois (não aberta como issue formal, só registrada aqui).
- **Especificação:** [`_docs/specs/features/separar-golive-diretoria/spec.md`](file:///Volumes/SSD_Externo/repo_personal/java-pleno-pixel-hunt/_docs/specs/features/separar-golive-diretoria/spec.md)
- **Design:** [`_docs/specs/features/separar-golive-diretoria/design.md`](file:///Volumes/SSD_Externo/repo_personal/java-pleno-pixel-hunt/_docs/specs/features/separar-golive-diretoria/design.md)
- **Tarefas:** [`_docs/specs/features/separar-golive-diretoria/tasks.md`](file:///Volumes/SSD_Externo/repo_personal/java-pleno-pixel-hunt/_docs/specs/features/separar-golive-diretoria/tasks.md)
- **Validação:** [`_docs/specs/features/separar-golive-diretoria/validation.md`](file:///Volumes/SSD_Externo/repo_personal/java-pleno-pixel-hunt/_docs/specs/features/separar-golive-diretoria/validation.md)

> Feature anterior `orquestracao-generica-de-fases` (branch `feat/orquestracao-generica-de-fases`) está **Verified (PASS) e já mergeada em `main`** (squash `a96799d`, confirmado como merge-base desta sessão) — ver `_docs/specs/features/orquestracao-generica-de-fases/` se precisar de histórico (16 requisitos `PHASEFLOW-01` a `16`, 1 ciclo de fix pós-code-review).
> Feature anterior `motor-de-jogo-modular-fix2` (branch `feat/motor-de-jogo-modular`, HEAD `193d804`) está **Verified (PASS)**, sem pendência bloqueante além de `npm run test:e2e` não rodado — ver `_docs/specs/features/motor-de-jogo-modular-fix2/` se precisar de histórico.
> Feature anterior `motor-de-jogo-modular-fix1` está **Verified (PASS)** — ver `_docs/specs/features/motor-de-jogo-modular-fix1/` se precisar de histórico (8 requisitos `ENGINE-16` a `ENGINE-23`, 2 ciclos de fix internos).
> Feature anterior `sistema-de-temas-hud` (branch `study/hud-redesign`) está **Verified (PASS)**, sem pendência bloqueante — ver `_docs/specs/features/sistema-de-temas-hud/` se precisar de histórico. Decisão pendente do usuário (não bloqueia a feature atual): mergear `study/hud-redesign` pra `main`, remediar achados do SonarQube ("new code" contando o branch inteiro), ou aposentar as rotas de estudo `/qwen/hud-redesign` e `/clude/hud-redesign`.
> Features anteriores `estabilidade-qualidade-fase2` e `estabilidade-qualidade` (branch `main`) também concluídas — ver suas pastas em `_docs/specs/features/` se precisar de histórico.
